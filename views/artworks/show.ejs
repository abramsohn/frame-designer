<!DOCTYPE html>
<main class="container h-100">
  <div class="row gx-5">
    <div class="col col-3 pt-md-5">
      <h1><%= artwork.title %></h1>
      <dl class="row">
        <dt class="col-sm-6">Image Size:</dt>
        <dd class="col-sm-6"><%=artwork.height%> X <%=artwork.width%></dd>
        <dt class="col-sm-6">border size</dt>
        <dd id="borders" class="col-sm-6"><%=artwork.borders%></dd>
        <a href="/artworks/<%= artwork.id %>/edit">Edit This Artwork</a>
        <a href="/artworks/<%= artwork.id %>?_method=DELETE"
          >Delete This Artwork</a
        >
      </dl>
    </div>

    <div class="col col-7 vh-75">
      <canvas id="designer" width="300" height="150">
        Unfortunately your browser does not support canvas.
      </canvas>
    </div>
  </div>
</main>
<script src="/js/designer-preview.js" defer></script>
